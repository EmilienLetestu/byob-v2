{% for key,info in data %}
    <ul>
        <li>
            {{ key }}  {{ info }}
        </li>
    </ul>
{% endfor %}

<div class="user-dashboard-alias">
    <a class="btn" href="{{ path('arrival') }}">nouvel arrivage</a>
</div>